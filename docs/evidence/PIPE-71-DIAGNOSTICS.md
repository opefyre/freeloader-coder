# PIPE-71 — Diagnostics evidence

Domain: `packages/control-center/src/diagnostics.ts`. Test: `tests/control-center-diagnostics.test.ts`. UI: `/` → Local doctor and support bundle.

Bundles are explicitly selected and previewed. Automated tests exclude source code and redact credentials, secrets, and user paths while retaining correlation and reproduction context.
