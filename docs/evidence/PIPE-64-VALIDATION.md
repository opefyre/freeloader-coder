# PIPE-64 — Validation evidence

Domain: `packages/validation/src/project-validation.ts`. Tests: `tests/quality-validation.test.ts`. UI: `/evidence`.

Proof covers changed-scope selection, required-gate retention, reproducible plan/report digests, explicit outcome taxonomy, readiness blocking, and consequence-bearing authorized waivers.
