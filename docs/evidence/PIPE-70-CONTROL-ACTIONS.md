# PIPE-70 — Safe actions evidence

Domain: `packages/control-center/src/actions.ts`. Test: `tests/control-center-actions.test.ts`. UI: `/` → Safe operator action.

Actions are canonical-state valid, idempotent, impact-previewed, and audited. Unsafe recovery names the blocker and smallest required decision.
