# PIPE-68 — Control metrics evidence

Source contract: `fixtures/control-center-metrics.ts` and `packages/control-center/src/metrics.ts`. Tests: `tests/operational-metrics.test.ts` and `tests/control-center-provider-share.test.ts`.

All displayed metric kinds retain provenance. Missing quota evidence stays unavailable. Provider share includes external and local calls from the selected range.
