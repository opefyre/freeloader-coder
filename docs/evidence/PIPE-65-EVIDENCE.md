# PIPE-65 — Evidence-browser evidence

Domain: `packages/validation/src/evidence-bundle.ts`. Tests: `tests/quality-evidence.test.ts`. UI: `/evidence` → Evidence browser.

Changed code requires diff and validation artifacts. Diffs, checks, builds, commits, logs, visual proof, and limitations retain distinct states and stable artifact references. Visual failure blocks UI changes only.
